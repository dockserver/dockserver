name: revase


on:
  workflow_dispatch:
  schedule:
  - cron: "0 */2 * * *"

jobs:
  rebase:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - uses: peter-evans/rebase@v2
        with:
          base: master
